{
  "name": "<%= name %>",
<% if (description) { -%>  "description": "<%= description %>",
<% } -%>
  "main": "<%= name %>.html",
  "version": "0.0.1",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "polymer serve",
    "test": "polymer test"
  },
  "devDependencies": {
    "web-component-tester": "^6.0.0-prerelease.5"
  }
}
